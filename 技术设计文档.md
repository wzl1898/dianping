# 高并发本地生活服务平台 — 技术设计文档

> **版本**：v1.0  
> **日期**：2026-06-10  
> **对应需求文档**：需求文档.md  
> **技术栈**：Spring Boot 2.7 + MySQL 8 + Redis + RabbitMQ + MinIO

---

## 目录

1. [总体架构](#1-总体架构)
2. [数据库设计](#2-数据库设计)
3. [Redis Key 设计规范](#3-redis-key-设计规范)
4. [API 接口设计](#4-api-接口设计)
5. [详细模块设计](#5-详细模块设计)
6. [RabbitMQ 设计](#6-rabbitmq-设计)
7. [MinIO 集成设计](#7-minio-集成设计)
8. [限流设计](#8-限流设计)
9. [统一响应与异常码](#9-统一响应与异常码)
10. [项目配置](#10-项目配置)

---

## 1. 总体架构

### 1.1 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                   客户端 / Postman                       │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP REST API
                       ▼
┌─────────────────────────────────────────────────────────┐
│                Nginx（反向代理，可选）                      │
└──────────────────────┬──────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│                  Spring Boot 应用                         │
│                                                          │
│  ┌────────────┐  ┌──────────┐  ┌──────────────────────┐ │
│  │ Interceptor │  │   AOP    │  │  Controller Layer    │ │
│  │ 登录/刷新   │  │  限流切面 │  │  REST API           │ │
│  └────────────┘  └──────────┘  └──────────┬───────────┘ │
│                                           │              │
│  ┌────────────────────────────────────────┴───────────┐ │
│  │              Service Layer                          │ │
│  │  UserService  │  ShopService  │  VoucherService    │ │
│  │  BlogService  │  FollowService│  SignService       │ │
│  └──────────────────────┬─────────────────────────────┘ │
│                         │                                │
│  ┌──────────────────────┴─────────────────────────────┐ │
│  │            Data / Middleware Layer                   │ │
│  │  ┌──────┐  ┌───────┐  ┌─────────┐  ┌───────────┐  │ │
│  │  │ MySQL │  │ Redis │  │RabbitMQ │  │  MinIO    │  │ │
│  │  │       │  │       │  │         │  │ 对象存储  │  │ │
│  │  └──────┘  └───────┘  └─────────┘  └───────────┘  │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 1.2 包结构

```
com.dianping
├── DianpingApplication.java
├── config/
│   ├── RedisConfig.java              # Redis 序列化配置
│   ├── MyBatisPlusConfig.java        # MyBatis-Plus 分页插件
│   ├── RabbitMQConfig.java           # 交换机、队列声明
│   ├── MinIOConfig.java              # MinIO 客户端配置
│   ├── WebMvcConfig.java             # CORS、拦截器注册
│   └── JacksonConfig.java            # JSON 序列化配置
├── common/
│   ├── Result.java                   # 统一响应封装
│   ├── GlobalExceptionHandler.java   # 全局异常处理
│   ├── SystemConstants.java          # 系统常量
│   └── ErrorCode.java                # 错误码枚举
├── annotation/
│   └── RateLimiter.java              # 限流注解
├── aop/
│   └── RateLimiterAspect.java        # 限流切面
├── interceptor/
│   ├── LoginInterceptor.java         # 登录校验拦截器
│   └── RefreshTokenInterceptor.java  # Token 刷新拦截器
├── utils/
│   ├── RedisIdWorker.java            # 分布式 ID 生成器
│   ├── RegexUtils.java               # 手机号正则校验
│   ├── MinIOUploadUtil.java          # MinIO 上传工具
│   └── UserHolder.java               # ThreadLocal 持有者
├── entity/
│   ├── User.java
│   ├── Shop.java
│   ├── ShopType.java
│   ├── Voucher.java
│   ├── SeckillVoucher.java
│   ├── VoucherOrder.java
│   ├── Blog.java
│   ├── Follow.java
│   └── Admin.java
├── dto/
│   ├── LoginFormDTO.java             # 登录表单
│   ├── UserDTO.java                  # 脱敏用户信息
│   ├── SeckillVoucherDTO.java        # 秒杀券展示
│   └── BlogQueryDTO.java             # 笔记查询参数
├── vo/
│   ├── ShopVO.java                   # 商户视图（含距离）
│   ├── BlogVO.java                   # 笔记视图（含作者信息）
│   └── UserVO.java                   # 用户视图
├── mapper/
│   ├── UserMapper.java
│   ├── ShopMapper.java
│   ├── ShopTypeMapper.java
│   ├── VoucherMapper.java
│   ├── VoucherOrderMapper.java
│   ├── BlogMapper.java
│   ├── FollowMapper.java
│   └── AdminMapper.java
├── service/
│   ├── IUserService.java
│   ├── IShopService.java
│   ├── IShopTypeService.java
│   ├── IVoucherService.java
│   ├── ISeckillVoucherService.java
│   ├── IVoucherOrderService.java
│   ├── IBlogService.java
│   ├── IFollowService.java
│   ├── ISignService.java
│   ├── IUvService.java
│   └── IAdminService.java
│   └── impl/
│       ├── UserServiceImpl.java
│       ├── ShopServiceImpl.java
│       ├── ShopTypeServiceImpl.java
│       ├── VoucherServiceImpl.java
│       ├── SeckillVoucherServiceImpl.java
│       ├── VoucherOrderServiceImpl.java
│       ├── BlogServiceImpl.java
│       ├── FollowServiceImpl.java
│       ├── SignServiceImpl.java
│       ├── UvServiceImpl.java
│       └── AdminServiceImpl.java
└── controller/
    ├── UserController.java
    ├── ShopController.java
    ├── ShopTypeController.java
    ├── VoucherController.java
    ├── BlogController.java
    ├── FollowController.java
    ├── SignController.java
    ├── UploadController.java
    └── admin/
        ├── AdminShopController.java
        ├── AdminVoucherController.java
        └── AdminUserController.java
```

---

## 2. 数据库设计

### 2.1 DDL 脚本

```sql
-- ============================================================
-- 1. 用户表
-- ============================================================
CREATE TABLE `tb_user` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '用户ID',
    `phone`       VARCHAR(11)  NOT NULL COMMENT '手机号',
    `password`    VARCHAR(128)     NULL COMMENT '密码（预留）',
    `nick_name`   VARCHAR(32)  NOT NULL DEFAULT '' COMMENT '昵称',
    `icon`        VARCHAR(500) NOT NULL DEFAULT '' COMMENT '头像URL',
    `create_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- ============================================================
-- 2. 商户分类表
-- ============================================================
CREATE TABLE `tb_shop_type` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '分类ID',
    `name`        VARCHAR(32)  NOT NULL COMMENT '分类名称',
    `icon`        VARCHAR(500) NOT NULL DEFAULT '' COMMENT '图标URL',
    `sort`        INT          NOT NULL DEFAULT 0 COMMENT '排序权重（升序）',
    `create_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商户分类表';

-- ============================================================
-- 3. 商户表
-- ============================================================
CREATE TABLE `tb_shop` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '商户ID',
    `name`        VARCHAR(128) NOT NULL COMMENT '商户名称',
    `type_id`     BIGINT       NOT NULL COMMENT '分类ID',
    `images`      VARCHAR(1024) NOT NULL DEFAULT '' COMMENT '图片URL（多张逗号分隔）',
    `area`        VARCHAR(64)  NOT NULL DEFAULT '' COMMENT '所在区域',
    `address`     VARCHAR(256) NOT NULL DEFAULT '' COMMENT '详细地址',
    `x`           DOUBLE       NOT NULL DEFAULT 0 COMMENT '经度',
    `y`           DOUBLE       NOT NULL DEFAULT 0 COMMENT '纬度',
    `avg_price`   BIGINT       NOT NULL DEFAULT 0 COMMENT '均价（单位：分）',
    `sold`        INT          NOT NULL DEFAULT 0 COMMENT '销量',
    `comments`    INT          NOT NULL DEFAULT 0 COMMENT '评论数',
    `score`       INT          NOT NULL DEFAULT 0 COMMENT '评分（0-50，乘以10存储）',
    `open_hours`  VARCHAR(64)  NOT NULL DEFAULT '' COMMENT '营业时间',
    `create_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_type_id` (`type_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商户表';

-- ============================================================
-- 4. 优惠券表
-- ============================================================
CREATE TABLE `tb_voucher` (
    `id`           BIGINT       NOT NULL AUTO_INCREMENT COMMENT '优惠券ID',
    `shop_id`      BIGINT       NOT NULL COMMENT '所属商户ID',
    `title`        VARCHAR(128) NOT NULL COMMENT '标题',
    `sub_title`    VARCHAR(256) NOT NULL DEFAULT '' COMMENT '副标题',
    `rules`        VARCHAR(512) NOT NULL DEFAULT '' COMMENT '使用规则',
    `pay_value`    BIGINT       NOT NULL COMMENT '支付金额（单位：分）',
    `actual_value` BIGINT       NOT NULL COMMENT '实际价值（单位：分）',
    `type`         TINYINT      NOT NULL DEFAULT 0 COMMENT '类型：0-普通券，1-秒杀券',
    `status`       TINYINT      NOT NULL DEFAULT 1 COMMENT '状态：0-下架，1-上架',
    `create_time`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_shop_id` (`shop_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='优惠券表';

-- ============================================================
-- 5. 秒杀券额外信息表
-- ============================================================
CREATE TABLE `tb_seckill_voucher` (
    `voucher_id`  BIGINT   NOT NULL COMMENT '关联优惠券ID',
    `stock`       INT      NOT NULL COMMENT '库存',
    `begin_time`  DATETIME NOT NULL COMMENT '秒杀开始时间',
    `end_time`    DATETIME NOT NULL COMMENT '秒杀结束时间',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`voucher_id`),
    KEY `idx_begin_end` (`begin_time`, `end_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='秒杀券信息表';

-- ============================================================
-- 6. 订单表
-- ============================================================
CREATE TABLE `tb_voucher_order` (
    `id`          BIGINT   NOT NULL COMMENT '订单ID（分布式ID生成）',
    `user_id`     BIGINT   NOT NULL COMMENT '用户ID',
    `voucher_id`  BIGINT   NOT NULL COMMENT '优惠券ID',
    `pay_type`    TINYINT  NOT NULL DEFAULT 1 COMMENT '支付类型：1-余额，2-微信（预留）',
    `status`      TINYINT  NOT NULL DEFAULT 1 COMMENT '订单状态：1-未支付，2-已支付（预留）',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `pay_time`    DATETIME     NULL COMMENT '支付时间（预留）',
    `use_time`    DATETIME     NULL COMMENT '使用时间（预留）',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_voucher` (`user_id`, `voucher_id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_voucher_id` (`voucher_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';

-- ============================================================
-- 7. 探店笔记表
-- ============================================================
CREATE TABLE `tb_blog` (
    `id`          BIGINT        NOT NULL AUTO_INCREMENT COMMENT '笔记ID',
    `shop_id`     BIGINT        NOT NULL COMMENT '关联商户ID',
    `user_id`     BIGINT        NOT NULL COMMENT '作者ID',
    `title`       VARCHAR(32)   NOT NULL COMMENT '标题',
    `images`      VARCHAR(2048) NOT NULL DEFAULT '' COMMENT '图片URL（多张逗号分隔）',
    `content`     VARCHAR(2048) NOT NULL DEFAULT '' COMMENT '文字内容',
    `liked`       INT           NOT NULL DEFAULT 0 COMMENT '点赞数（冗余字段）',
    `comments`    INT           NOT NULL DEFAULT 0 COMMENT '评论数（预留）',
    `create_time` DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    KEY `idx_shop_id` (`shop_id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='探店笔记表';

-- ============================================================
-- 8. 关注关系表
-- ============================================================
CREATE TABLE `tb_follow` (
    `id`             BIGINT   NOT NULL AUTO_INCREMENT COMMENT '关系ID',
    `user_id`        BIGINT   NOT NULL COMMENT '关注者ID',
    `follow_user_id` BIGINT   NOT NULL COMMENT '被关注用户ID',
    `create_time`    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_follow` (`user_id`, `follow_user_id`),
    KEY `idx_follow_user` (`follow_user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='关注关系表';

-- ============================================================
-- 9. 管理员表
-- ============================================================
CREATE TABLE `tb_admin` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '管理员ID',
    `username`    VARCHAR(32)  NOT NULL COMMENT '用户名',
    `password`    VARCHAR(128) NOT NULL COMMENT '密码（BCrypt加密）',
    `nickname`    VARCHAR(32)  NOT NULL DEFAULT '' COMMENT '昵称',
    `create_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='管理员表';
```

### 2.2 ER 关系

```
tb_user (1) ─────< (N) tb_follow (N) >───── (1) tb_user
  │                                               │
  │                                               │
  └────< (N) tb_blog                              │
  │                │                              │
  │                └──> (1) tb_shop               │
  │                                               │
  └────< (N) tb_voucher_order >──── (1) tb_voucher
                                              │
                                              │
                                    (1) tb_seckill_voucher
```

---

## 3. Redis Key 设计规范

### 3.1 Key 命名规范

格式：`业务域:对象ID:用途`

| Key 模式 | 类型 | 用途 | 有效期 |
|----------|------|------|--------|
| `login:code:{phone}` | String | 验证码 | 5min |
| `login:token:{token}` | Hash | 登录用户信息 | 30min |
| `cache:shop:{id}` | String | 商户详情缓存 | 30min + 随机偏移 |
| `cache:shop:type` | String | 商户分类列表 | 30min |
| `cache:shop:{id}:null` | String | 缓存空对象 | 5min |
| `seckill:stock:{voucherId}` | String | 秒杀券 Redis 库存 | 秒杀结束 |
| `seckill:order:{voucherId}` | Set | 已秒杀用户集合 | 秒杀结束 |
| `seckill:user:{userId}:{voucherId}` | String | 一人一单锁标记 | 5s |
| `order:id:gen` | String | 分布式ID序列 | 永久 |
| `blog:liked:{blogId}` | Set | 笔记点赞用户集合 | 永久 |
| `blog:liked:rank:{blogId}` | ZSet | 点赞排行榜 | 永久 |
| `follow:{userId}` | Set | 用户关注列表 | 永久 |
| `fan:{userId}` | Set | 用户粉丝列表 | 永久 |
| `feed:{userId}` | ZSet | 用户收件箱 | 永久 |
| `shop:geo:{typeId}` | GEO | 某分类商户坐标 | 永久 |
| `sign:{userId}:{yyyyMM}` | BitMap | 用户月签到 | 永久 |
| `uv:day:{yyyyMMdd}` | HyperLogLog | 每日UV | 30天 |
| `lock:hot:{id}` | String | 互斥锁 | 10s |
| `rate:limit:{type}:{key}` | ZSet | 滑动窗口限流记录 | 窗口期 |

### 3.2 序列化策略

| Redis 类型 | 序列化方案 |
|-----------|-----------|
| String | Jackson JSON |
| Hash | Jackson JSON（field 为字段名） |
| Set | 直接存用户ID(Long) |
| ZSet | member=笔记ID(Long), score=时间戳 |
| GEO | 直接存坐标 |
| BitMap | 直接位操作 |
| HyperLogLog | 直接 PFADD |

---

## 4. API 接口设计

### 4.1 用户模块

#### 发送验证码

```
POST /api/user/code
Content-Type: application/json

Request:
{
    "phone": "13800138000"
}

Response:
{
    "success": true
}
```

#### 登录/注册

```
POST /api/user/login
Content-Type: application/json

Request:
{
    "phone": "13800138000",
    "code": "123456"
}

Response:
{
    "success": true,
    "data": "a1b2c3d4-...-..."  // Token
}
```

#### 获取当前用户

```
GET /api/user/me
Authorization: Bearer {token}

Response:
{
    "success": true,
    "data": {
        "id": 1,
        "nickName": "张三",
        "icon": "http://...",
        "phone": "138****8000"
    }
}
```

#### 退出登录

```
POST /api/user/logout
Authorization: Bearer {token}

Response:
{
    "success": true
}
```

### 4.2 商户模块

#### 查询商户分类列表

```
GET /api/shop-type/list

Response:
{
    "success": true,
    "data": [
        {"id": 1, "name": "美食", "icon": "http://...", "sort": 1},
        {"id": 2, "name": "电影", "icon": "http://...", "sort": 2}
    ]
}
```

#### 按分类分页查询商户

```
GET /api/shop/list?typeId=1&current=1&x=116.3&y=39.9

Response:
{
    "success": true,
    "data": {
        "records": [
            {
                "id": 1,
                "name": "海底捞",
                "typeId": 1,
                "images": "...",
                "avgPrice": 15000,
                "score": 45,
                "distance": "1.2km"     // 按距离排序时返回
            }
        ],
        "total": 100,
        "current": 1
    }
}
```

#### 查询商户详情

```
GET /api/shop/{id}

Response:
{
    "success": true,
    "data": {
        "id": 1,
        "name": "海底捞",
        "typeId": 1,
        "images": "...",
        "area": "朝阳区",
        "address": "建国路88号",
        "x": 116.3,
        "y": 39.9,
        "avgPrice": 15000,
        "sold": 5000,
        "comments": 200,
        "score": 45,
        "openHours": "10:00-22:00"
    }
}
```

#### 更新商户信息

```
PUT /api/admin/shop
Content-Type: application/json

Request:
{
    "id": 1,
    "name": "海底捞",
    "typeId": 1,
    "images": "...",
    "area": "朝阳区",
    "address": "建国路88号",
    "x": 116.3,
    "y": 39.9,
    "avgPrice": 15000,
    "openHours": "10:00-22:00"
}

Response:
{
    "success": true
}
```

### 4.3 优惠券秒杀模块

#### 新增优惠券

```
POST /api/admin/voucher
Content-Type: application/json

Request（普通券）:
{
    "shopId": 1,
    "title": "满100减20",
    "subTitle": "全场通用",
    "rules": "仅限堂食",
    "payValue": 8000,
    "actualValue": 10000,
    "type": 0
}

Request（秒杀券）:
{
    "shopId": 1,
    "title": "5折秒杀券",
    "subTitle": "限量50张",
    "payValue": 5000,
    "actualValue": 10000,
    "type": 1,
    "stock": 50,
    "beginTime": "2026-06-11T10:00:00",
    "endTime": "2026-06-11T23:59:59"
}

Response:
{
    "success": true,
    "data": 1001   // 优惠券ID
}
```

#### 查询商户优惠券

```
GET /api/voucher/list/{shopId}

Response:
{
    "success": true,
    "data": [
        {
            "id": 1001,
            "shopId": 1,
            "title": "满100减20",
            "subTitle": "全场通用",
            "payValue": 8000,
            "actualValue": 10000,
            "type": 0
        },
        {
            "id": 1002,
            "shopId": 1,
            "title": "5折秒杀券",
            "subTitle": "限量50张",
            "payValue": 5000,
            "actualValue": 10000,
            "type": 1,
            "stock": 50,
            "beginTime": "2026-06-11T10:00:00",
            "endTime": "2026-06-11T23:59:59"
        }
    ]
}
```

#### 秒杀下单

```
POST /api/voucher/seckill/{voucherId}
Authorization: Bearer {token}

Response:
{
    "success": true,
    "data": 123456789   // 订单ID（分布式ID）
}
```

#### 查询订单

```
GET /api/voucher/order/{orderId}

Response:
{
    "success": true,
    "data": {
        "id": 123456789,
        "voucherId": 1001,
        "status": 1,
        "createTime": "2026-06-10T12:00:00"
    }
}
```

### 4.4 探店笔记模块

#### 发布笔记

```
POST /api/blog
Authorization: Bearer {token}
Content-Type: application/json

Request:
{
    "shopId": 1,
    "title": "超好吃的火锅",
    "images": "http://minio:9000/dianping-blog/xxx.jpg",
    "content": "这家店真的绝了..."
}

Response:
{
    "success": true,
    "data": 101   // 笔记ID
}
```

#### 分页查询笔记

```
GET /api/blog?current=1

Response:
{
    "success": true,
    "data": {
        "records": [
            {
                "id": 101,
                "shopId": 1,
                "userId": 1,
                "title": "超好吃的火锅",
                "images": "...",
                "content": "这家店真的绝了...",
                "liked": 42,
                "createTime": "2026-06-10T12:00:00",
                "author": {
                    "id": 1,
                    "nickName": "张三",
                    "icon": "http://..."
                },
                "isLiked": true   // 当前登录用户是否已点赞
            }
        ],
        "total": 200,
        "current": 1
    }
}
```

#### 笔记详情

```
GET /api/blog/{id}

Response: 同上 data 中单条记录
```

#### 点赞/取消点赞

```
PUT /api/blog/like/{id}
Authorization: Bearer {token}

Response:
{
    "success": true
}
```

#### 点赞排行榜 TOP5

```
GET /api/blog/likes/{id}

Response:
{
    "success": true,
    "data": [
        {"id": 2, "nickName": "李四", "icon": "http://..."},
        {"id": 3, "nickName": "王五", "icon": "http://..."}
    ]
}
```

### 4.5 关注模块

#### 关注/取关

```
PUT /api/follow/{userId}/{isFollow}
Authorization: Bearer {token}
// isFollow: true=关注, false=取关

Response:
{
    "success": true
}
```

#### 查询是否已关注

```
GET /api/follow/or/not/{userId}
Authorization: Bearer {token}

Response:
{
    "success": true,
    "data": true   // 已关注
}
```

#### 共同关注

```
GET /api/follow/common/{userId}
Authorization: Bearer {token}

Response:
{
    "success": true,
    "data": [
        {"id": 3, "nickName": "王五", "icon": "http://..."},
        {"id": 5, "nickName": "赵六", "icon": "http://..."}
    ]
}
```

#### 查询收件箱（Feed 流）

```
GET /api/blog/follow?lastId=1686384000000&offset=0
Authorization: Bearer {token}

// lastId: 上一次查询的最小时间戳
// offset: 偏移量（用于处理同一时间戳多条数据）

Response:
{
    "success": true,
    "data": {
        "records": [
            {"id": 101, "title": "...", "author": {...}, "createTime": "..."},
            ...
        ],
        "minTime": 1686383000000,   // 本次查询的最小时间戳（下次传入 lastId）
        "offset": 2                 // 该时间戳的第几条（下次传入 offset）
    }
}
```

### 4.6 附近商户

#### 按类型+距离查询商户

```
GET /api/shop/list/nearby?typeId=1&x=116.3&y=39.9&distance=5000&current=1

// distance: 范围（米），默认5000

Response:
{
    "success": true,
    "data": {
        "records": [
            {
                "id": 1,
                "name": "海底捞",
                "typeId": 1,
                "distance": "1200m",
                "x": 116.32,
                "y": 39.91
            }
        ],
        "current": 1
    }
}
```

### 4.7 签到模块

#### 用户签到

```
POST /api/sign
Authorization: Bearer {token}

Response:
{
    "success": true
}
```

#### 签到统计

```
GET /api/sign/count
Authorization: Bearer {token}

Response:
{
    "success": true,
    "data": {
        "total": 15,          // 当月签到总次数
        "continuous": 5       // 连续签到天数
    }
}
```

### 4.8 UV 统计

#### 获取 UV

```
GET /api/uv?date=2026-06-10

Response:
{
    "success": true,
    "data": 12345
}
```

### 4.9 文件上传

#### 上传图片

```
POST /api/upload
Authorization: Bearer {token}
Content-Type: multipart/form-data

Request:
    file: <binary>

Response:
{
    "success": true,
    "data": "http://localhost:9000/dianping-blog/xxx.jpg"
}
```

### 4.10 管理后台

#### 管理员登录

```
POST /api/admin/login
Content-Type: application/json

Request:
{
    "username": "admin",
    "password": "password"
}

Response:
{
    "success": true,
    "data": "admin:token:xxx"   // 管理员 Token
}
```

#### 商户管理（CRUD）

```
GET    /api/admin/shop?current=1&name=海底捞     # 分页查询
POST   /api/admin/shop                             # 新增（见商户模块请求体）
PUT    /api/admin/shop                             # 修改（见商户模块请求体）
DELETE /api/admin/shop/{id}                        # 删除
```

#### 优惠券管理（CRUD）

```
GET    /api/admin/voucher?page=1&shopId=1           # 分页查询
POST   /api/admin/voucher                           # 新增
DELETE /api/admin/voucher/{id}                      # 删除
```

#### 用户管理

```
GET /api/admin/user?current=1&phone=13800138000     # 分页查询
```

---

## 5. 详细模块设计

### 5.1 用户认证与拦截器链

#### 5.1.1 拦截器链结构

```
请求到达
    │
    ▼
┌────────────────────────────────────────────┐
│ RefreshTokenInterceptor (order=0)          │
│ - 所有请求都通过                           │
│ - 从请求头获取 Token                       │
│ - 到 Redis 查询 Token 是否存在             │
│ - 存在：刷新 TTL，用户信息存入 ThreadLocal │
│ - 不存在：不拦截，直接放行                │
│ - 请求结束后清除 ThreadLocal              │
└───────────────────┬────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────┐
│ LoginInterceptor (order=1)                 │
│ - 拦截需要登录的路径                       │
│ - 从 ThreadLocal 获取用户信息              │
│ - 存在：放行                              │
│ - 不存在：返回 401 未登录                 │
└───────────────────┬────────────────────────┘
                    │
                    ▼
                Controller
```

#### 5.1.2 ThreadLocal 工具

```java
public class UserHolder {
    private static final ThreadLocal<UserDTO> TL = new ThreadLocal<>();
    
    public static void saveUser(UserDTO user) { TL.set(user); }
    public static UserDTO getUser() { return TL.get(); }
    public static void remove() { TL.remove(); }
}
```

#### 5.1.3 Token 刷新逻辑

- 每次请求 → RefreshTokenInterceptor 从 Redis 查到 Token → 执行 `EXPIRE key 1800` 续期
- 若 Token 已过期 → 不拦截，后续业务若需登录由 LoginInterceptor 返回 401
- 若 Token 在过期前持续使用 → 一直续期，避免用户使用中突然掉线

### 5.2 二级缓存设计

#### 5.2.1 架构

```
请求到达 ShopService
    │
    ▼
┌──────────────────┐
│   Caffeine 本地   │  ← 第一级，极快，无网络开销
│   缓存            │
└──────┬───────────┘
       │ miss
       ▼
┌──────────────────┐
│   Redis 分布式    │  ← 第二级，集群共享
│   缓存            │
└──────┬───────────┘
       │ miss
       ▼
┌──────────────────┐
│   MySQL 数据库    │  ← 兜底
│                  │
└──────────────────┘
```

#### 5.2.2 缓存击穿方案

**方案 A：互斥锁**

```
查询缓存 → 未命中 → 尝试获取锁(SETNX)
    ├── 获取成功 → 查 DB → 重建缓存 → 释放锁
    └── 获取失败 → 休眠重试 → 查缓存
```

**方案 B：逻辑过期**

```
缓存中存储数据 + 逻辑过期时间（expireTime）
查询缓存 → 取出数据
    ├── 未过期 → 直接返回
    └── 已过期 → 获取互斥锁
        ├── 获取成功 → 新线程异步重建缓存 → 返回旧数据
        └── 获取失败 → 返回旧数据（不等待）
```

#### 5.2.3 缓存一致性

采用 **Cache Aside 模式**：

```
更新 DB → 删除 Redis 缓存（非更新）→ 
下次查询 → 缓存 miss → 查 DB → 重建缓存
```

搭配 TTL 自动兜底，即使缓存删除失败，最终也会因 TTL 过期而自动清理。

### 5.3 秒杀系统详细设计

#### 5.3.1 分布式 ID 生成器

```java
@Component
public class RedisIdWorker {
    // 起始时间戳：2026-01-01 00:00:00
    private static final long BEGIN_TIMESTAMP = 1767225600L;
    // 序列号位数
    private static final int COUNT_BITS = 32;
    
    @Autowired
    private StringRedisTemplate redisTemplate;
    
    public long nextId(String keyPrefix) {
        // 生成时间戳
        LocalDateTime now = LocalDateTime.now();
        long timestamp = now.toEpochSecond(ZoneOffset.UTC) - BEGIN_TIMESTAMP;
        
        // 生成序列号（按天自增，方便统计）
        String date = now.format(DateTimeFormatter.ofPattern("yyyy:MM:dd"));
        Long count = redisTemplate.opsForValue()
            .increment("order:id:gen:" + keyPrefix + ":" + date);
        
        // 拼接：1bit符号位0 + 31bit时间戳 + 32bit序列号
        return timestamp << COUNT_BITS | count;
    }
}
```

#### 5.3.2 Lua 脚本（秒杀资格预检）

```lua
-- 参数列表
local voucherId = ARGV[1]
local userId = ARGV[2]

-- 数据 key
local stockKey = 'seckill:stock:' .. voucherId
local orderKey = 'seckill:order:' .. voucherId

-- 1. 判断库存是否充足
if (tonumber(redis.call('get', stockKey)) or 0) <= 0 then
    return 1   -- 库存不足
end

-- 2. 判断用户是否已秒杀过
if redis.call('sismember', orderKey, userId) == 1 then
    return 2   -- 重复下单
end

-- 3. 扣减库存，记录用户
redis.call('decr', stockKey)
redis.call('sadd', orderKey, userId)

return 0   -- 秒杀资格获取成功
```

#### 5.3.3 异步下单流程（V8 最终版）

```
用户请求秒杀
    │
    ▼
(1) Lua 脚本预检（库存+去重）
    │
    ├── 失败 → 返回错误（库存不足/重复下单）
    │
    └── 成功
        │
        ▼
(2) 创建订单对象（分布式ID）
        │
        ▼
(3) 发送到 RabbitMQ
        │
        ▼
(4) 返回"下单成功"给用户（立即响应，< 30ms）
        │
        ▼
(5) MQ 消费者
    ├── 获取订单消息
    ├── 幂等性校验（订单ID作为唯一键 INSERT IGNORE）
    └── 写入 MySQL 订单表
```

#### 5.3.4 幂等性保证

- 订单 ID 是主键（分布式 ID 全局唯一）
- 使用 `INSERT IGNORE` 或 `INSERT ... ON DUPLICATE KEY UPDATE`
- 重复消息消费时直接忽略，保证最终一致性

### 5.4 Feed 流滚动分页

#### 5.4.1 推模式流程

```
博主发布笔记
    │
    ▼
(1) 写入 MySQL tb_blog 表
    │
    ▼
(2) 查询博主的粉丝列表（可从 Redis 粉丝 Set 查）
    │
    ▼
(3) 遍历粉丝，将笔记ID + 时间戳写入粉丝的收件箱：
    ZADD feed:{followerId} {timestamp} {blogId}
```

#### 5.4.2 滚动分页查询

```
难点：传统 LIMIT offset, size 在 Feed 流中会有数据错乱
（新数据插入导致分页偏移）

解决：基于时间戳 + 偏移量的滚动分页

用户查询收件箱：
    ZREVRANGEBYSCORE feed:{userId} max min WITHSCORES LIMIT offset count
    
    - max: 上次查询的最小时间戳（首次用当前时间）
    - min: 0
    - offset: 上次查询中该时间戳的偏移量（首次传 0）
    - count: 每页条数（如 3）
    
    返回数据中：
    - minTime: 本次查询的最小 score（下次请求的 max）
    - offset: 该 score 值的第几条（用于处理同时间戳的多条数据）
```

### 5.5 附近商户 GEO 查询

#### 5.5.1 数据写入时机

```
商户创建或更新坐标时：
    GEOADD shop:geo:{typeId} {x} {y} {shopId}
```

#### 5.5.2 查询流程

```
用户请求附近商户（含经纬度、范围、类型）
    │
    ▼
(1) 构建 GEO 查询参数
    ├── 中心点：用户经纬度 (x, y)
    ├── 半径：distance（默认 5000m）
    └── 类型：typeId
    │
    ▼
(2) Redis 查询：
    GEOSEARCH shop:geo:{typeId} 
        FROMLONLAT {x} {y}
        BYRADIUS {distance} m
        WITHCOORD
        WITHDIST
    │
    ▼
(3) 返回(shopId, distance, x, y) 列表，按距离升序
    │
    ▼
(4) 根据 shopId 批量查询 MySQL 获取详细信息
    │
    ▼
(5) 组装 VO 返回前端
```

### 5.6 签到 BitMap 设计

#### 5.6.1 签到存储

```java
// 签到：将当天对应的 bit 置为 1
// key: sign:1:202606（用户ID:年月）
// offset: dayOfMonth - 1（6月10日 => offset=9）
redisTemplate.opsForValue().setBit(
    "sign:" + userId + ":" + yearMonth, 
    dayOfMonth - 1, 
    true
);
```

#### 5.6.2 连续签到统计

```java
// 1. 获取当月签到 BitMap（返回 byte[]）
// 2. 从当天开始，逐位向前检查
// 3. 遇到 0 则停止计数
// 4. 统计连续的 1 的个数

// 关键：将 byte[] 转为二进制位串，按位与 + 右移
for (int i = dayOfMonth - 1; i >= 0; i--) {
    boolean signed = (bits[i / 8] >> (i % 8) & 1) == 1;
    if (signed) count++;
    else break;
}
```

### 5.7 UV 统计

```java
// 每日 UV：每个访客（IP/用户ID）加入 HyperLogLog
redisTemplate.opsForHyperLogLog().add("uv:day:20260610", userId);

// 查询 UV
Long uv = redisTemplate.opsForHyperLogLog().size("uv:day:20260610");
```

---

## 6. RabbitMQ 设计

### 6.1 交换机与队列

| 交换机 | 类型 | 队列 | Routing Key | 说明 |
|--------|------|------|-------------|------|
| `seckill.direct` | Direct | `seckill.order.queue` | `seckill.order` | 秒杀订单 |

### 6.2 配置声明

```java
@Configuration
public class RabbitMQConfig {
    
    public static final String SECKILL_EXCHANGE = "seckill.direct";
    public static final String SECKILL_QUEUE = "seckill.order.queue";
    public static final String SECKILL_ROUTING_KEY = "seckill.order";
    
    @Bean
    public Queue seckillQueue() {
        return QueueBuilder.durable(SECKILL_QUEUE).build();
    }
    
    @Bean
    public DirectExchange seckillExchange() {
        return new DirectExchange(SECKILL_EXCHANGE);
    }
    
    @Bean
    public Binding seckillBinding() {
        return BindingBuilder.bind(seckillQueue())
            .to(seckillExchange())
            .with(SECKILL_ROUTING_KEY);
    }
}
```

### 6.3 消息体格式

```json
{
    "voucherId": 1001,
    "userId": 1,
    "orderId": 123456789,
    "createTime": "2026-06-10T12:00:00"
}
```

### 6.4 消费者

```java
@Component
@Slf4j
public class SeckillOrderConsumer {
    
    @Autowired
    private IVoucherOrderService voucherOrderService;
    
    @RabbitListener(queues = "seckill.order.queue")
    public void handleSeckillOrder(VoucherOrderMessage message) {
        log.debug("消费秒杀订单: {}", message.getOrderId());
        voucherOrderService.createOrder(message);
    }
}
```

---

## 7. MinIO 集成设计

### 7.1 配置

```yaml
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  buckets:
    shop: dianping-shop
    blog: dianping-blog
    avatar: dianping-avatar
```

### 7.2 Bucket 初始化

```java
@Configuration
public class MinIOConfig {
    
    @Bean
    public MinioClient minioClient(MinioProperties properties) {
        MinioClient client = MinioClient.builder()
            .endpoint(properties.getEndpoint())
            .credentials(properties.getAccessKey(), properties.getSecretKey())
            .build();
        // 初始化 Bucket
        for (String bucket : properties.getBuckets().values()) {
            if (!client.bucketExists(BucketExistsArgs.builder().bucket(bucket).build())) {
                client.makeBucket(MakeBucketArgs.builder().bucket(bucket).build());
                // 设为公开读
                client.setBucketPolicy(...);
            }
        }
        return client;
    }
}
```

### 7.3 上传 API 实现

```java
@RestController
@RequestMapping("/api/upload")
public class UploadController {
    
    @PostMapping
    public Result<String> upload(@RequestParam("file") MultipartFile file) {
        // 1. 校验文件类型（jpg/png/gif）
        // 2. 生成唯一文件名（UUID + 后缀）
        // 3. 上传到 MinIO（按业务类型分 Bucket）
        // 4. 返回可访问 URL
        String url = minioUploadUtil.upload(file, "dianping-blog");
        return Result.ok(url);
    }
}
```

---

## 8. 限流设计

### 8.1 自定义注解

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimiter {
    /**
     * 限流键前缀
     */
    String key() default "rate:limit";
    
    /**
     * 限流时间窗口（秒）
     */
    int time() default 5;
    
    /**
     * 窗口内最大请求数
     */
    int count() default 5;
    
    /**
     * 限流类型：IP / USER
     */
    LimitType limitType() default LimitType.IP;
    
    enum LimitType {
        IP, USER
    }
}
```

### 8.2 滑动窗口算法

```java
@Aspect
@Component
public class RateLimiterAspect {
    
    @Around("@annotation(rateLimiter)")
    public Object around(ProceedingJoinPoint pjp, RateLimiter rateLimiter) throws Throwable {
        // 1. 构造限流 key
        String key = buildKey(rateLimiter);
        
        // 2. 滑动窗口 ZSet 记录
        long now = System.currentTimeMillis();
        long windowStart = now - rateLimiter.time() * 1000L;
        
        // 3. 清理窗口外的旧记录
        redisTemplate.opsForZSet().removeRangeByScore(key, 0, windowStart);
        
        // 4. 统计窗口内请求数
        Long count = redisTemplate.opsForZSet().zCard(key);
        
        if (count >= rateLimiter.count()) {
            return Result.error("请求过于频繁，请稍后再试");
        }
        
        // 5. 记录本次请求
        redisTemplate.opsForZSet().add(key, String.valueOf(now), now);
        
        // 6. 设置过期时间（防止内存泄漏）
        redisTemplate.expire(key, rateLimiter.time(), TimeUnit.SECONDS);
        
        return pjp.proceed();
    }
}
```

### 8.3 使用示例

```java
@RestController
@RequestMapping("/api/voucher")
public class VoucherController {
    
    @RateLimiter(key = "seckill", time = 5, count = 3, limitType = LimitType.USER)
    @PostMapping("/seckill/{voucherId}")
    public Result<Long> seckill(@PathVariable Long voucherId) {
        // 秒杀逻辑
    }
}
```

---

## 9. 统一响应与异常码

### 9.1 统一响应体

```java
@Data
@NoArgsConstructor
@AllArgsConstructor
public class Result<T> {
    private Boolean success;
    private T data;
    private String errorMsg;
    private Integer total;  // 分页查询时使用
    
    public static <T> Result<T> ok(T data) {
        return new Result<>(true, data, null, null);
    }
    
    public static <T> Result<T> ok(T data, Integer total) {
        return new Result<>(true, data, null, total);
    }
    
    public static <T> Result<T> error(String errorMsg) {
        return new Result<>(false, null, errorMsg, null);
    }
    
    public static <T> Result<T> error(ErrorCode errorCode) {
        return new Result<>(false, null, errorCode.getMessage(), null);
    }
}
```

### 9.2 错误码定义

```java
public enum ErrorCode {
    
    // 通用（10000-10099）
    SUCCESS(10000, "成功"),
    UNKNOWN_ERROR(10001, "未知错误"),
    PARAM_ERROR(10002, "参数错误"),
    
    // 用户（10100-10199）
    USER_NOT_LOGIN(10100, "未登录"),
    INVALID_CODE(10101, "验证码错误"),
    CODE_EXPIRED(10102, "验证码已过期"),
    CODE_TOO_FREQUENT(10103, "发送过于频繁"),
    INVALID_PHONE(10104, "手机号格式错误"),
    
    // 秒杀（10200-10299）
    SECKILL_STOCK_INSUFFICIENT(10200, "库存不足"),
    SECKILL_REPEAT_ORDER(10201, "重复下单"),
    SECKILL_NOT_STARTED(10202, "秒杀未开始"),
    SECKILL_ENDED(10203, "秒杀已结束"),
    SECKILL_BUSY(10204, "秒杀太火爆，请稍后再试"),
    RATE_LIMITED(10205, "请求过于频繁，请稍后再试"),
    
    // 订单（10300-10399）
    ORDER_NOT_FOUND(10300, "订单不存在"),
    
    // 管理端（10400-10499）
    ADMIN_AUTH_FAIL(10400, "管理员认证失败"),
    
    // 文件（10500-10599）
    FILE_TYPE_ERROR(10500, "不支持的文件类型"),
    FILE_TOO_LARGE(10501, "文件过大");
    
    private final int code;
    private final String message;
    
    // constructor + getter
}
```

### 9.3 全局异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusiness(BusinessException e) {
        return Result.error(e.getErrorCode());
    }
    
    @ExceptionHandler(Exception.class)
    public Result<?> handleUnknown(Exception e) {
        log.error("未知异常", e);
        return Result.error(ErrorCode.UNKNOWN_ERROR);
    }
}
```

---

## 10. 项目配置

### 10.1 application.yml

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dianping?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 16
        max-idle: 8
        min-idle: 4

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        prefetch: 1
        concurrency: 4
        max-concurrency: 8

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.dianping.entity
  global-config:
    db-config:
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  buckets:
    shop: dianping-shop
    blog: dianping-blog
    avatar: dianping-avatar

# SpringDoc 文档（springdoc-openapi-ui v1.6.x）
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
```

### 10.2 Redis 序列化配置

```java
@Configuration
public class RedisConfig {
    
    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(factory);
        
        // JSON 序列化
        Jackson2JsonRedisOptions options = Jackson2JsonRedisOptions...
        GenericJackson2JsonRedisSerializer serializer = 
            new GenericJackson2JsonRedisSerializer();
        
        // String key
        template.setKeySerializer(RedisSerializer.string());
        template.setHashKeySerializer(RedisSerializer.string());
        
        // JSON value
        template.setValueSerializer(serializer);
        template.setHashValueSerializer(serializer);
        
        template.afterPropertiesSet();
        return template;
    }
}
```

---

## 附录：各版本秒杀对比总表

| 版本 | 核心方案 | 库存原子性 | 一人一单 | 分布式 | 响应时间 | 吞吐量 |
|------|---------|-----------|---------|--------|---------|-------|
| V1 | SQL 查询+更新 | ❌ | ❌ | ❌ | ~200ms | ~200 |
| V2 | 乐观锁 stock>0 | ✅ | ❌ | ✅ | ~150ms | ~400 |
| V3 | V2 + DB 唯一键 | ✅ | ✅ | ✅ | ~150ms | ~400 |
| V4 | V3 + synchronized | ✅ | ✅ | ❌(单机) | ~80ms | ~800 |
| V5 | SETNX 分布式锁 | ✅ | ✅ | ✅ | ~50ms | ~1500 |
| V6 | Redisson 看门狗 | ✅ | ✅ | ✅ | ~50ms | ~1500 |
| V7 | Lua 脚本原子化 | ✅ | ✅ | ✅ | ~10ms | ~3000 |
| V8 | V7 + RabbitMQ 异步 | ✅ | ✅ | ✅ | <5ms(预检) | >3000 |

---

> **参考**：本文档基于《黑马点评》教学项目的技术设计逻辑，结合本项目的扩展需求编写而成。
