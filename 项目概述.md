高并发本地生活服务平台
项目描述：
仿大众点评的本地生活服务平台，涵盖用户认证、商户查询、优惠券秒杀、地理位置搜索、社交互动等核心业务。针对高并发场景下的缓存一致性、超卖、重复下单等痛点进行了深度优化，实现了高可用、高性能的服务架构。
项目内容：
(1). 高性能缓存策略：针对热点商品数据，采用caffeine本地缓存与redis搭建二级缓存架构，提高热点数据访问速度。使用逻辑过期+独立线程重建的方式防止缓存击穿，使用布隆过滤器防止缓存穿透，使用随机TTL的方法防止缓存雪崩。
(2). 秒杀系统优化：设计基于 Redis 的分布式 ID 生成器，解决分库分表场景下的全局唯一 ID 问题；使用 Redis + Lua 脚本实现秒杀资格预检；采用乐观锁替代悲观锁兜底超卖问题，将并发竞争粒度从表级降至行级；通过 RabbitMQ 实现异步下单，将秒杀接口响应时间从 500ms 降至 50ms 以内。
(3). 滑动窗口限流：使用Redis+AOP+注解的方式实现基于IP/用户id的限流机制，用以抵御爬虫和黄牛脚本，防止系统过载，保障用户权益。
(4). 数据一致性保障：采用Cache Aside方法来保持缓存一致性，并使用TTL兜底。使用订单id唯一性约束来保障异步建单的消息幂等性。

