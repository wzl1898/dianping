# 高并发本地生活服务平台 — 需求文档

> **版本**：v1.0  
> **日期**：2026-06-10  
> **定位**：学习研究项目（仿大众点评 / 黑马点评）  
> **技术栈**：Spring Boot 2.7 + MySQL + Redis + RabbitMQ + MinIO（纯后端 API）
>
> **版本规划**：
> - **v1（当前）**：单库部署，聚焦业务功能完整性与高并发场景优化
> - **v2（规划）**：分库分表 + 读写分离，提升系统扩展能力

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [总体范围](#2-总体范围)
3. [功能需求](#3-功能需求)
   - 3.1 用户认证模块
   - 3.2 商户查询缓存模块
   - 3.3 优惠券秒杀模块
   - 3.4 达人探店（社交互动）模块
   - 3.5 好友关注模块
   - 3.6 附近商户模块
   - 3.7 用户签到模块
   - 3.8 UV 统计模块
   - 3.9 后台管理模块
4. [非功能需求](#4-非功能需求)
5. [技术架构](#5-技术架构)
6. [数据模型概要](#6-数据模型概要)

---

## 1. 项目背景与目标

### 1.1 项目背景

本地生活服务平台是连接消费者与线下商户的重要桥梁。消费者通过平台发现附近商户、查看评价、获取优惠，并完成消费决策。此类平台面临高并发访问、缓存一致性、秒杀超卖、防爬虫限流等经典技术挑战。

### 1.2 项目目标

- 构建一个仿大众点评的本地生活服务后端 API 平台
- 深度实践高并发场景下的缓存策略、秒杀优化、限流防护等技术方案
- 实现完整的"发现商户 → 查看评价 → 下单消费 → 社交互动"业务闭环

### 1.3 项目定位

本项目为**学习研究型项目**，采用 **Spring Boot 2.7 + MySQL + Redis + RabbitMQ + MinIO** 技术栈，为纯后端 API 项目，本地部署运行。侧重 Redis 多数据结构的实战应用及高并发场景下的性能优化。

---

## 2. 总体范围

### 2.1 功能范围

| 模块 | 包含功能 | 优先级 |
|------|---------|--------|
| 用户认证 | 手机号验证码登录/注册、Token 登录态维护 | P0 |
| 商户缓存 | 商户分类浏览、商户详情查询、缓存三大问题防护 | P0 |
| 优惠券秒杀 | 秒杀资格预检、一人一单、异步下单、分布式ID | P0 |
| 探店社交 | 发布探店笔记、点赞、点赞排行榜 | P1 |
| 好友关注 | 关注/取关、共同关注、Feed 流推送 | P1 |
| 附近商户 | 基于坐标的距离搜索、按距离排序 | P1 |
| 用户签到 | 每日签到、连续签到天数统计 | P2 |
| UV 统计 | 每日独立访客统计 | P2 |
| 后台管理 | 商户管理、优惠券管理、数据概览 | P2 |
| 限流防护 | 基于 IP/用户的滑动窗口限流 | P0 |

### 2.2 非功能范围

| 包含 | 不包含 |
|------|--------|
| 短信验证码模拟（控制台输出） | 真实短信通道对接 |
| 异步下单（RabbitMQ） | 真实支付对接 |
| 本地单机部署 | 云端部署、容器化编排 |
| 基于 Redis 的限流 | 网关层限流（如 Sentinel） |

---

## 3. 功能需求

---

### 3.1 用户认证模块

#### 3.1.1 业务流程

```
用户输入手机号 → 后端生成验证码 → 存入 Redis（5 分钟过期）→ 
用户输入验证码 → 校验通过 → 查询或创建用户 → 
生成随机 Token → 用户信息以 Hash 存入 Redis（30 分钟过期，可续期）→
返回 Token 给前端
```

#### 3.1.2 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-1 | 发送验证码 | 接收手机号，生成 6 位随机验证码，存入 Redis（key=`login:code:{phone}`），控制台输出验证码 |
| UC-2 | 验证码登录/注册 | 校验手机号+验证码，通过后查询用户表，不存在则自动创建新用户；生成 UUID Token（key=`login:token:{token}`），以 Hash 存储用户基本信息 |
| UC-3 | 校验登录态 | 请求携带 Token，拦截器校验，通过后将用户信息存入 ThreadLocal |
| UC-4 | 刷新 Token 有效期 | 双层拦截器：第一层拦截所有请求，若请求携带有效 Token 则续期 30 分钟；第二层拦截需登录路径 |
| UC-5 | 退出登录 | 删除 Redis 中的 Token 记录 |

#### 3.1.3 关键约束

- 验证码有效期：5 分钟
- Token 有效期：30 分钟（每次请求自动续期）
- 同一手机号 60 秒内不能重复发送验证码

---

### 3.2 商户查询缓存模块

#### 3.2.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-6 | 商户分类查询 | 查询所有商户类型列表，结果缓存到 Redis |
| UC-7 | 商户分页查询 | 按分类查询商户列表，支持分页 |
| UC-8 | 商户详情查询 | 根据 ID 查询商户详细信息，使用缓存加速 |

#### 3.2.2 缓存策略

针对缓存三大问题，提供两套可选方案（均在项目中实现）：

| 问题 | 方案 A（互斥锁） | 方案 B（逻辑过期） |
|------|-----------------|-------------------|
| **缓存穿透** | 查询为空时缓存空对象（短 TTL） | 布隆过滤器前置拦截 |
| **缓存击穿** | `SETNX` 互斥锁，获锁线程查库重建缓存 | 逻辑过期标记，独立线程异步重建 |
| **缓存雪崩** | TTL 添加随机值 | 同左 |

- **一致性策略**：Cache Aside 模式（先更新数据库，再删除缓存）+ TTL 自动兜底

---

### 3.3 优惠券秒杀模块

#### 3.3.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-9 | 分布式 ID 生成 | 基于 Redis 自增 + 时间戳，生成全局唯一订单 ID |
| UC-10 | 秒杀资格预检 | Redis + Lua 脚本原子判断：库存充足 + 用户未购买 |
| UC-11 | 异步下单 | 预检成功后写入消息队列，后台线程异步创建数据库订单 |
| UC-12 | 订单查询 | 查询用户订单信息 |

#### 3.3.2 版本演进设计

体现学习过程，模块按以下版本递进实现：

| 版本 | 目标 | 方案 |
|------|------|------|
| V1 | 基础下单 | 直接操作数据库 |
| V2 | 解决超卖 | 乐观锁 `update ... where stock > 0` |
| V3 | 一人一单 | 数据库唯一约束 + 判断是否已下单 |
| V4 | 单机并发 | `synchronized` + `userId.intern()` |
| V5 | 分布式锁 | Redis `SETNX` + Lua 释放锁 |
| V6 | 分布式锁增强 | Redisson 看门狗 |
| V7 | 原子化预检 | Redis + Lua 脚本一次完成 |
| V8 | 异步下单 | Lua 预检 → RabbitMQ → 异步建单 |

#### 3.3.3 关键约束

- 秒杀券每人限购 1 张
- 订单表使用分布式 ID，保证全局唯一
- 异步建单需保证幂等性（通过订单 ID 唯一约束）

---

### 3.4 达人探店模块

#### 3.4.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-13 | 发布探店笔记 | 用户发布图文笔记，图片上传至 MinIO，返回 URL 存储到数据库 |
| UC-14 | 笔记分页查询 | 按时间倒序分页展示 |
| UC-15 | 笔记详情 | 查询单条笔记内容 |
| UC-16 | 点赞/取消 | Redis Set 存储，每人只能点一次，再点取消 |
| UC-17 | 点赞排行榜 | Redis SortedSet（以时间戳为 score），展示前 5 名 |

---

### 3.5 好友关注模块

#### 3.5.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-18 | 关注/取关 | 数据库关系表 + Redis Set 缓存 |
| UC-19 | 查询是否已关注 | 批量查询关注关系 |
| UC-20 | 共同关注 | Redis `SINTER` 求交集 |
| UC-21 | Feed 流推送 | 博主发布笔记，推送到粉丝收件箱（SortedSet） |
| UC-22 | 滚动分页 | 基于时间戳 + 偏移量的滚动分页查询收件箱 |

#### 3.5.2 Feed 流策略

采用**推模式（写扩散）**，博主发布笔记时将笔记 ID 推送到所有粉丝的 SortedSet 收件箱中。

---

### 3.6 附近商户模块

#### 3.6.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-23 | GEO 数据写入 | 商户创建/更新时将坐标写入 Redis GEO |
| UC-24 | 按距离搜索 | 根据用户经纬度，按距离搜索商户并按距离排序 |
| UC-25 | 按类型+距离搜索 | 在指定商户类型内按距离搜索 |

#### 3.6.2 技术方案

- Redis `GEOADD` 存储商户坐标
- `GEOSEARCH` 命令实现距离查询与排序（需 Redis 6.2+）

---

### 3.7 用户签到模块

#### 3.7.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-26 | 签到 | 使用 BitMap 存储（key=`sign:{userId}:{yyyyMM}`） |
| UC-27 | 当月签到次数 | 统计当月签到记录 |
| UC-28 | 连续签到 | 从当天往前逐位检查，统计连续天数 |

---

### 3.8 UV 统计模块

#### 3.8.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-29 | UV 记录 | 使用 HyperLogLog 统计每日独立访客 |
| UC-30 | UV 查询 | 查询指定日期的 UV |

---

### 3.9 后台管理模块

#### 3.9.1 功能列表

| 编号 | 功能 | 说明 |
|------|------|------|
| UC-31 | 商户管理 | 商户及分类的增删改查 |
| UC-32 | 优惠券管理 | 发布优惠券/秒杀券，设置库存、有效期、秒杀时间 |
| UC-33 | 用户管理 | 用户列表查询 |
| UC-34 | 数据概览 | 基本运营数据展示 |

#### 3.9.2 管理端鉴权

- 独立的管理员账号体系，与用户端 Token 隔离

---

## 4. 非功能需求

### 4.1 性能要求

| 指标 | v1 目标 | v2 目标（分库分表后） |
|------|---------|----------------------|
| 秒杀接口响应时间（异步后） | < 50ms | < 30ms |
| 商户详情查询（缓存命中） | < 10ms | < 5ms |
| 商户详情查询（缓存穿透/回源DB） | < 200ms（95分位） | < 150ms（95分位） |
| 缓存命中率 | > 90% | > 95% |
| 秒杀下单吞吐量（单机） | > 3000 TPS | > 5000 TPS |

### 4.2 压力测试规划

使用 Apache JMeter 对核心接口进行压力测试，验证优化效果并记录性能基线。

#### 4.2.1 测试场景

| 编号 | 场景 | 测试目标 | 对比项 |
|------|------|---------|--------|
| PT-1 | 商户详情查询 | 缓存命中率、响应时间 | 有缓存 vs 无缓存 |
| PT-2 | 秒杀资格预检 | Lua 脚本原子化性能 | V1(裸DB) ~ V8(异步) 各版本对比 |
| PT-3 | 秒杀异步下单 | RabbitMQ 吞吐能力 | 同步下单 vs 异步下单 |
| PT-4 | 限流效果验证 | 滑动窗口限流拦截准确率 | 有限流 vs 无限流 |
| PT-5 | 登录 Token 校验 | 拦截器性能开销 | 有拦截器 vs 直通 |
| PT-6 | 附近商户查询 | GEO 查询性能 | Redis GEO vs MySQL 经纬度计算 |
| PT-7 | 混合场景 | 综合业务流量下的系统表现 | 各接口按比例混合施压 |

#### 4.2.2 测试指标

- **吞吐量**：QPS / TPS
- **响应时间**：P50、P95、P99 分位值
- **错误率**：异常响应占比
- **缓存命中率**：Redis 缓存命中统计
- **资源监控**：CPU、内存、网络 IO

#### 4.2.3 测试工具配置

- **工具**：Apache JMeter 5.x
- **测试脚本位置**：项目根目录下 `jmeter/` 目录维护各场景 JMX 文件
- **数据准备**：通过 SQL 脚本预置 1000+ 商户、10000+ 用户、多张秒杀券等基础数据
- **测试策略**：阶梯加压（从 100 并发逐步升至 5000 并发），每个阶梯持续 30 秒，观察系统拐点
- **报告输出**：JMeter HTML 报告，标注优化前/后对比

#### 4.2.4 各版本秒杀性能对比（预期）

| 版本 | 方案 | 预期 QPS | 预期 P99 |
|------|------|---------|---------|
| V1 | 裸 DB 操作 | ~200 | ~800ms |
| V2 | 乐观锁 | ~400 | ~400ms |
| V3 | 一人一单+乐观锁 | ~400 | ~400ms |
| V4 | JVM 锁 | ~800 | ~200ms |
| V5 | Redis SETNX 分布式锁 | ~1500 | ~120ms |
| V6 | Redisson 看门狗 | ~1500 | ~120ms |
| V7 | Lua 脚本原子化 | ~3000 | ~50ms |
| V8 | Lua + RabbitMQ 异步 | > 3000 | < 30ms |

#### 4.2.5 测试目录结构

```
jmeter/
├── scenarios/
│   ├── PT-1-shop-query.jmx
│   ├── PT-2-seckill-precheck.jmx
│   ├── PT-3-seckill-async.jmx
│   ├── PT-4-rate-limiter.jmx
│   ├── PT-5-login-token.jmx
│   ├── PT-6-nearby-shop.jmx
│   └── PT-7-mixed-scenario.jmx
├── data/
│   └── test-data.sql            # 测试数据准备脚本
├── report/                      # 测试报告输出目录
└── README.md                    # 压测使用说明

### 4.3 安全与防护

- 验证码防刷：60 秒内同一手机号限制
- 滑动窗口限流：基于 IP 和用户 ID，使用 Redis + AOP + 注解实现，**重点保护秒杀接口**
- 缓存穿透防护：布隆过滤器 / 缓存空对象

### 4.4 API 规范

- RESTful 风格
- 统一响应格式 `Result<T>`
- 全局异常处理
- SpringDoc OpenAPI 文档

---

## 5. 技术架构

### 5.1 技术栈

| 层次 | 选型 |
|------|------|
| 框架 | Spring Boot 2.7.x, Spring MVC |
| ORM | MyBatis-Plus |
| 数据库 | MySQL 8.x |
| 缓存 | Redis (Lettuce), Caffeine (本地二级缓存) |
| 分布式锁 | Redisson |
| 消息队列 | RabbitMQ |
| 对象存储 | MinIO |
| 限流 | Redis + AOP + 自定义注解 |
| API 文档 | SpringDoc OpenAPI (springdoc-openapi-ui v1.6.x) |
| 性能测试 | Apache JMeter |
| 工具库 | HuTool, Lombok |

### 5.2 项目结构

```
dianping
├── src/main/java/com/dianping
│   ├── DianpingApplication.java
│   ├── config/           # 配置类（Redis、MyBatis-Plus、Jackson、CORS、MinIO、RabbitMQ）
│   ├── common/           # 公共组件（Result、异常处理、常量）
│   ├── annotation/       # 自定义注解（限流 @RateLimiter 等）
│   ├── aop/              # AOP 切面（限流切面实现）
│   ├── interceptor/      # 拦截器（登录刷新拦截器、登录校验拦截器）
│   ├── utils/            # 工具类（RedisIdWorker、MinIOClientUtil、RegexUtils 等）
│   ├── entity/           # 数据实体
│   ├── dto/              # 数据传输对象
│   ├── vo/               # 视图对象
│   ├── mapper/           # MyBatis-Plus Mapper 接口
│   ├── service/          # 服务接口
│   │   └── impl/         # 服务实现
│   └── controller/       # 控制器（用户端 + 管理端）
└── src/main/resources/
    ├── application.yml
    └── mapper/           # XML Mapper（如有）
```

### 5.3 接口设计基础路径

| 范围 | 路径 |
|------|------|
| 用户端 API | `/api/user/**`, `/api/shop/**`, `/api/voucher/**` 等 |
| 管理端 API | `/api/admin/**` |
| 文件上传 | `/api/upload/**`（上传至 MinIO，返回可访问 URL） |

### 5.4 MinIO 存储设计

- **部署方式**：本地 Docker 部署 MinIO 服务
- **Bucket 规划**：
  - `dianping-shop` — 商户图片
  - `dianping-blog` — 探店笔记图片
  - `dianping-avatar` — 用户头像
- **上传流程**：客户端 → API 上传接口 → MinIO 服务端 → 返回文件访问 URL → 存入数据库对应字段
- **访问控制**：MinIO Bucket 设为公开读，写操作通过后端 API 鉴权后执行

---

## 6. 数据模型概要

### 6.1 核心表结构

#### tb_user（用户）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 用户 ID |
| phone | varchar(11) UK | 手机号 |
| password | varchar | 密码 |
| nick_name | varchar(32) | 昵称 |
| icon | varchar(500) | 头像 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### tb_shop（商户）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 商户 ID |
| name | varchar(128) | 名称 |
| type_id | bigint | 分类 ID |
| images | varchar | 图片（逗号分隔） |
| area | varchar | 地区 |
| address | varchar | 地址 |
| x | double | 经度 |
| y | double | 纬度 |
| avg_price | bigint | 均价（分） |
| sold | int | 销量 |
| comments | int | 评论数 |
| score | int | 评分（0-50） |
| open_hours | varchar | 营业时间 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### tb_shop_type（商户分类）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 分类 ID |
| name | varchar(32) | 名称 |
| icon | varchar | 图标 |
| sort | int | 排序 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### tb_voucher（优惠券）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 优惠券 ID |
| shop_id | bigint | 所属商户 |
| title | varchar | 标题 |
| sub_title | varchar | 副标题 |
| rules | varchar | 使用规则 |
| pay_value | bigint | 支付金额（分） |
| actual_value | bigint | 实际价值（分） |
| type | tinyint | 0=普通券，1=秒杀券 |
| status | tinyint | 状态 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### tb_seckill_voucher（秒杀券）

| 字段 | 类型 | 说明 |
|------|------|------|
| voucher_id | bigint PK | 关联优惠券 ID |
| stock | int | 库存 |
| begin_time | datetime | 秒杀开始时间 |
| end_time | datetime | 秒杀结束时间 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### tb_voucher_order（订单）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 分布式全局唯一 ID |
| user_id | bigint | 用户 ID |
| voucher_id | bigint | 优惠券 ID |
| pay_type | tinyint | 支付类型（预留） |
| status | tinyint | 订单状态 |
| create_time | datetime | 创建时间 |
| pay_time | datetime | 支付时间（预留） |
| use_time | datetime | 使用时间（预留） |
| update_time | datetime | 更新时间 |

#### tb_blog（探店笔记）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 笔记 ID |
| shop_id | bigint | 关联商户 |
| user_id | bigint | 作者 ID |
| title | varchar(32) | 标题 |
| images | varchar | 图片（逗号分隔） |
| content | varchar(2048) | 内容 |
| liked | int | 点赞数 |
| comments | int | 评论数（预留） |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### tb_follow（关注关系）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 关系 ID |
| user_id | bigint | 关注者 |
| follow_user_id | bigint | 被关注用户 |
| create_time | datetime | 创建时间 |

#### tb_admin（管理员）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 管理员 ID |
| username | varchar(32) UK | 用户名 |
| password | varchar | 密码 |
| nickname | varchar(32) | 昵称 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

---

## 附录

### A. Redis 数据结构使用映射

| Redis 结构 | 应用场景 |
|-----------|---------|
| String | 验证码、缓存数据、Token |
| Hash | 用户信息缓存 |
| Set | 点赞集合、关注关系 |
| SortedSet | 点赞排行榜、Feed 流收件箱 |
| GEO | 附近商户 |
| BitMap | 签到 |
| HyperLogLog | UV 统计 |

### B. 版本规划

| 版本 | 范围 | 目标 |
|------|------|------|
| v1（当前） | 单库部署，本文档所述全部功能 | 业务功能完备，高并发场景优化落地 |
| v2（规划） | 分库分表 + 读写分离 | 提升系统扩展能力与数据容量 |

---

> **参考**：本需求文档参考了黑马程序员《黑马点评》教学项目的业务设计，并在此基础上做了扩展与调整。
